package compose;

public class Compose {

//	public static Rota rota(Linha linha, Node origem, Node destino, BigDecimal distancia) {
//		return Rota.builder().linha(linha).origem(origem).destino(destino).distancia(new BigDecimal(10);
//	}
}
